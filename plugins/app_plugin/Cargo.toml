[package]
name = "app_plugin"
version = "0.1.0"
publish = false
edition = "2021"

[features]
default = [
    "bevy/dynamic",
]

[dependencies]
bevy = "0.6.0"
bevy-inspector-egui = "0.7.2"
bevy_terrain = { path = "../bevy_terrain" }
bevy_fly_camera = "0.8.0"