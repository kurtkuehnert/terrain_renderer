[package]
name = "app_plugin"
version = "0.1.0"
publish = false
edition = "2021"

[features]
default = [
    "bevy/dynamic",
]

[dependencies]
bevy = { git = "https://github.com/bevyengine/bevy", branch = "latest" }
bevy-inspector-egui = { git = "https://github.com/jakobhellermann/bevy-inspector-egui", branch = "main" }
bevy_terrain = { path = "../bevy_terrain" }